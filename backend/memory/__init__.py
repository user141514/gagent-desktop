"""Memory tools and SOPs (previously imported via sys.path hack).

This file was added to make ``memory/`` a proper Python package.
Historical ``sys.path.append('../memory')`` hacks in various scripts
continue to work; new code should ``from memory import ljqCtrl`` etc.
"""
