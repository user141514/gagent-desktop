# Optional dependencies for real opt-in E2E checks.
# Keep separate from requirements-desktop.txt so normal packaged launches stay small.
openai-agents==0.18.0
browser-use==0.11.13
playwright==1.61.0
